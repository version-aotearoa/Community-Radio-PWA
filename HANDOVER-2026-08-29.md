# Handover — 2026-08-29

## Session outcome

Full brutalist redesign (Stitch "Version Radio Underground" system) shipped, player/archive work landed, brand identity traced, and a major boot-time regression fixed. Prod (version-radio.pages.dev) and GitHub (`main`) are in sync at `dedc893`; working tree clean; `npm run check` clean.

## What shipped today (commits 895910d → dedc893)

### Design system/brand
- Brutalist tokens + fonts (Anton / Hanken Grotesk / JetBrains Mono, self-hosted @fontsource) in `src/app.css`; zero radius, 1px white borders, monochrome + functional red/green status stickers
- Traced logo: `static/version-logo.svg` — pixel-exact trace of `version_logo.gif` (transparent, black shapes; alpha = glyph mask), letters hollow 2px outlines, **V = the canonical brand mark** (evenodd band from `logo.svg` geometry — identical shape; no double stroke). Reproducible: `node scripts/trace-version-logo.mjs`. Header: logo only; footer: single logo
- Favicon: black-tile stepped-V (`static/favicon.svg`) + regenerated icons + explicit icon/apple-touch/manifest links; `static/sw.js` cache bumped to `vr-static-v2`
- Copy: "Sign in > My Version" (signed-in → `/account`), "Aotearoa", mobile view-all under Latest Shows, hero/player full-colour images

### Features
- **Account** (`/account`): identity card, **Following** (follow_show) + **Saved broadcasts** (saved_episode), Sign out; revalidates on mount; footer Sign out added (never unreachable). Anonymous → `/login`. Fix included: `created_at` → `createdAt` (better-auth camelCase, ISO→epoch)
- **Follow/bookmark model**: show-level = Follow (bell), episode-level = Bookmark (broadcast) — `follow_show` (renamed from `saved_show`) + `saved_episode` (migration 0013, local+remote); context-aware UI (no bell on past/archives), player show contexts, sign-in prompts inline
- **Show pages**: upcoming rows header-only (no links to unaired episodes), past rows REPLAY/VIEW SHOW → only (REPLAY SOON removed); home Featured Shows (3 curated archive episodes w/ episode art + dates; fallback oldest-3); Latest Shows card → exact episode page + replay play buttons; Coming Up dates; date-ordered latest-show query (detunedradio fix)
- **Player**:
  - Source-switch fix (`prevKey` guard — regression fixed in `dedc893`: was `''` → auto-started live engine on load); autoplay opt-in (default OFF) + switch UI; loading trace on play buttons (reversed direction) that can't stick (clears on timeupdate/pause/seeked/play-reject)
  - **Archive controls**: ±30s rewind/forward (max player; forward clamped at live point), Media Session metadata + play/pause + ±30 actions (locked to 30s), exact **m4a `mvhd` duration probe** via new `/api/replay-info/[trackId]` (HEAD content-length + 1MB faststart scan; measured-slice only as last resort) — verified: reasonable dubs → 230,905,081 bytes, 7289s ≈ 2h 01m
  - **Same-origin HLS proxy** `/api/stream/[...path]` (hls/media/station prefixes only; ACAO *) — hls.js XHR no longer CORS-blocked from any origin (live playback now works pages.dev/LAN/localhost)
  - Fail-safes: play() rejection clears spinner + destroys engine; HLS fatal retries with backoff then stop

### Other
- Mobile menu: tap-outside close (document pointerdown — covers player expand too)
- Chat: identity/handle below the window, meta row + full-width content, mobile bubbles full-width
- Slugs: `scrape-` strip (0009), `version-excursions-26-8-5` → `version-excursions` (0011, refs only), **0012** re-pointed orphaned `track.broadcast_id` (tracklists restored), 0013 follow/saved-episodes — all applied to **local + remote** D1
- Local dev gotcha: `vite dev` + platform works via adapter (LAN `--host 0.0.0.0` for phone tests); `pages:dev` (:8788) for pre-deploy build checks

## Known open items
1. **Live-streamed archives: time in max player still not confirmed** — implemented (uncommitted): `isArchive` gate (any non-`live.m3u8` source = archive), est fallback chain on playback share `durationMinutes` (scheduled length) → `/api/replay-info` probe = m4a `mvhd` → **ID3v2 `TLEN`** (MP3; verified detunedradio → 7648s) → measured-slice last resort; clock `elapsed / ≈est`, ±30, positionState, Media Session seek driven by `estDuration`. **Untested: run LAN/iPhone loop (`window.__vrplayer()`, `[vr]` logs), then deploy.** Note: `prev-detunedradio`/`prev-version-excursions` replays are dead (AzuraCast returns text/html; files pruned) — REPLAY button for those is inert.
2. Debug instrumentation in StreamPlayer (reason-tagged `[vr] loading → true/false (reason)` logs, `audio:*` event spies, `window.__vrplayer()`) is **shipping** (useful for the iPhone loop — keep until the archive-time issue closes, then optionally strip)
3. `static/wordmark.svg` + `scripts/gen-wordmark.mjs` unused (superseded by version-logo trace) — safe to delete or keep
4. `version_logo.gif` stays in repo as trace source
5. Optional future: waveform in player (parked), PWA push for follow (parked), genre tags (no data)

## Commands
```sh
npm run check                      # svelte-check
npm run pages:dev                  # :8788 production-build preview
npm run dev -- --host 0.0.0.0      # :5173 LAN (phone testing)
npm run pages:deploy               # prod
npx wrangler d1 execute version-radio-db --local|--remote --file migrations/00XX_*.sql
node scripts/trace-version-logo.mjs
```

## DB/remote notes
- Remote D1 access: `export CLOUDFLARE_ACCOUNT_ID=6f00a3bc33382599b284ed7a623807d9` before `--remote`
- Local D1 re-imported from prod earlier (matches remote incl. replays); migrations 0009–0013 applied to both
- user table column is `createdAt` (better-auth camelCase) — only account loader touches it
